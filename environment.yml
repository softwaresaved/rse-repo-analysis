name: rse-repo-study
channels:
  - anaconda
  - defaults
dependencies:
  - pandas
  - pip
  - matplotlib
  - jupyter
  - lxml
  - pip:
      - beautifulsoup4
      - emoji
      - levenshtein
      - pdfminer-six
      - pydriller
      - pygithub
      - pyyaml
      - tqdm
      - unidecode